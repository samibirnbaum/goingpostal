<h1>Posts HomePage!! - All Posts</h1>
<%@posts.each do |post| %>
    <div class="media">
        <div class="media-body">
            <h4 class="media-heading">
                <%= link_to(post.title, post_path(post.id)) %>
            </h4>
        </div>
   </div> <!--an anchor tag, text is post.title and when you click it links to posts#show a path which needs a post id--> 
<% end %>
